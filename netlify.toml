[build]
  base = "frontend"
  publish = "frontend/.next"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "frontend/netlify/functions"
  node_bundler = "esbuild"
